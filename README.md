# 3113 Adventures – Sprint 10.0.1 Hotfix

- Cloud-Status zeigt jetzt den tatsächlichen Verbindungszustand.
- Gespeicherte Supabase-Konfiguration wird beim Start korrekt als eingerichtet angezeigt.
- Erfolgreiche Anmeldung wird nicht mehr als fehlgeschlagen gemeldet, wenn noch kein Cloud-Snapshot existiert.
- Beim ersten Gerät wird klar darauf hingewiesen, zuerst `Cloud speichern` zu verwenden.
- Cache-Version auf 10.0.1 aktualisiert.
