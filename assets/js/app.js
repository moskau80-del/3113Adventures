const navButtons=document.querySelectorAll('.main-nav button');const pages=document.querySelectorAll('.page');navButtons.forEach(button=>button.addEventListener('click',()=>{navButtons.forEach(item=>item.classList.toggle('active',item===button));pages.forEach(page=>page.classList.toggle('active',page.id===button.dataset.page));}));document.getElementById('refreshApp')?.addEventListener('click',async()=>{if('serviceWorker'in navigator){for(const registration of await navigator.serviceWorker.getRegistrations())await registration.unregister();}if('caches'in window){for(const name of await caches.keys())await caches.delete(name);}location.href='./?v=4011';});if('serviceWorker'in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('sw.js?v=4011'));}